---
"@jagreehal/sandbox-node": patch
---

Add a version command/flag: `sandbox version`, `sandbox -v`, and `sandbox --version` print the
installed sandbox version (previously `--version` errored with "unknown command").
